fix(v2.3.0): comprehensive project audit — complete SQL seed, fix PGDM collision, polish API & CLIs

CRITICAL FIXES:
- education_seed.sql: complete rewrite with 19 degree + 68 field INSERTs
  (was empty tables — unusable as a handoff artifact)
- generate_data.py: fix PGDM double-mapping to both MBA and PG Diploma;
  PGDM now correctly maps only to Post Graduate Diploma
- generate_data.py: replace nonsensical typo aliases with 25 realistic
  Indian resume misspellings
- README.md: fix phantom report/ and presentation/ directory references

API & CODE:
- app.py: HTTP 404 → 422 for unresolved inputs (semantically correct)
- app.py: add CORS middleware, /health endpoint, version bump to 2.2.0
- normalizer_tfidf.py, normalizer_embeddings.py: add full interactive
  CLI menus (were single-line print statements)
- All 4 PoC files: add module-level docstrings
- normalizer_rapidfuzz.py: remove unused import time, duplicate import os

DOCUMENTATION:
- CHANGELOG.md: normalize version format to consistent semver, fix stale
  normalizer.py reference, add v2.3.0 entry
- explainme.md: add cross-reference links and trailing separator
- requirements.txt: NEW — Python dependency manifest with version pins
- ppt_generation_prompt.md: NEW — 22-slide presentation generation brief
